const Navbar = () => {

  return(
    <header className="flex items-center w-full">
      <nav className="flex items-center w-full justify-between py-7 px-10">
        <div>
          Image
        </div>

        <div>
          <button>Sign In</button>
        </div>
      </nav>
    </header>
  )
}

export default Navbar;
